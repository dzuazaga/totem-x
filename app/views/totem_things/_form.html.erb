<%= form_for(@totem_thing) do |f| %>
  <% if @totem_thing.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@totem_thing.errors.count, "error") %> prohibited this totem_thing from being saved:</h2>

      <ul>
      <% @totem_thing.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :code %><br>
    <%= f.text_field :code %>
  </div>
  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
